## process_flow
Role: Describes the implementation lifecycle flow and/or the operational data flow through the OT cybersecurity tools. In the OTCSS domain this serves to illustrate either the staged project delivery methodology (Stage 0 through Stage 5 through post-implementation) or the runtime monitoring data flow (OT traffic → IDS sensor → core appliance → dashboards and alerts; vendor → SRA → OT host).
Required: [PLANT_LIST], [PRODUCTION_ZONE]; [ARCHITECTURE_OPTION] if confirmed.
Infer: Standard passive monitoring data path — OT traffic on network switches → SPAN/TAP → IDS sensor → encrypted channel → IDS core in DMZ → alerts and asset data → OT monitoring dashboards. Remote access path — external vendor → internet → SRA appliance (MFA/RBAC/approval) → approved session on OT host → session recording and audit log. Implementation lifecycle — Stage 0 (planning) → Stage 1 (discovery) → Stage 2 (architecture) → Stage 3 (tools deployment) → Stage 5 (policy/SOP/training) → post-implementation support.
Never invent: Specific protocol names, IP addresses, vendor product names, or network topology details not confirmed.
Structure: Brief narrative of the implementation lifecycle or the data monitoring flow (one to two paragraphs). If architecture option is confirmed, reference the corresponding network path. End with: "The above is for reference only and is subject to finalization during detailed engineering."