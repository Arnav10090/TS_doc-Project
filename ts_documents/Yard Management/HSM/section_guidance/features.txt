Purpose: Primary technical capability section. Lists and explains all CYMS/SYMS features included in the project scope. Must cover the full feature set from system objective through system architecture, features, system logic, integration, and value-added items.
Required Inputs: Confirmed feature scope for this project (CORE + confirmed OPTIONAL/COMMON); yard layout data ([NUM_COIL_YARD_BAYS], [NUM_SLAB_YARD_BAYS]); crane data ([NUM_CRANES_COIL], [NUM_CRANES_SLAB]); coil and slab dimensional/weight data (Buyer-supplied)
Safe Inferences: All CORE capabilities are always present and describable. Value-added items (anti-collision, zone movement, KPI dashboard, work order management, stacking optimization) are COMMON/OPTIONAL — include only if confirmed in scope. For each feature: name, description, operating mode (manual/auto), user role (operator/supervisor), integration dependency if applicable. System logic: Placement Logic (manual and auto) and Retrieval Logic steps. Integration table listing all confirmed integration targets. Crane hardware requirements: note as BUYER scope; position sensor accuracy [POSITION_SENSOR_ACCURACY_MM] (OBSERVED); network type TBD during engineering.
Must Not Invent: Specific stacking algorithm details or performance claims, specific KPI threshold or benchmark values, specific anti-collision detection parameters, specific report formats or field names, specific grid dimensions or capacity numbers not from Buyer data
Suggested Structure: Subsection 3.1 Overview → System Objective (bulleted list of 5 objectives) → System Architecture (three-tier description: crane subsystem as Buyer scope; CYMS/SYMS server; yard layout configuration) → Crane Hardware Requirements (Buyer scope note, OBSERVED specifications as guidance only) → CYMS & SYMS Features (bulleted list of all CORE + confirmed features) → System Logic (3.1a Placement Logic as numbered steps; 3.1b Retrieval Logic as numbered steps) → Integration (bulleted list of all confirmed integration targets; note MES/ERP as Future Scope) → Yard and Crane Data tables (populated from Buyer-supplied data in project metadata) → Value Added Items (numbered list; COMMON/OPTIONAL items only if confirmed). Following subsections: 3.1 Remote Support, 3.1.1 Documentation Control, 3.1.2 Customer Training.
AI Generation Rules: Do NOT duplicate shared knowledge from the context files.
