Purpose: Describes the system architecture configuration — the tiers, components, and deployment configuration of the CYMS/SYMS system. Text companion to the architecture diagram.
Required Inputs: [NUM_SERVERS], [NUM_CLIENT_PCS]; yard layout configuration data from project metadata; confirmed network approach (Wi-Fi or wired — TBD)
Safe Inferences: Three-tier architecture (Crane Subsystem / Application Server / Client Access); containerized deployment on online + backup server hardware; browser-based client access requiring no proprietary client software; configurable grid yard layout; API support for external integration; crane hardware (position sensors, WAPs, controllers) in BUYER scope
Must Not Invent: Specific IP addresses, hostname schemes, network topology with port details, server redundancy configurations beyond online+backup, specific API protocol details
Suggested Structure: 2-3 paragraph text description (image uploaded separately by user). (1) High-level three-tier architecture description. (2) Crane Subsystem configuration (Buyer scope elements: position sensors X/Y/Z, pick/drop sensors, onboard controller, HMI, WAPs/networking accessories). (3) CYMS/SYMS server and client configuration (online + backup server; client PCs; KVM extenders; browser-based access). End with: "The above configuration is for reference only and is subject to finalization during detailed engineering." Do not describe the diagram image — describe the architecture the image will show.
AI Generation Rules: Do NOT duplicate shared knowledge from the context files.
