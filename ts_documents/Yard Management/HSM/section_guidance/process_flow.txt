Purpose: Describes the operational workflow — how coils/slabs flow through the yard from production receipt to dispatch, and how CYMS/SYMS supports each step. Covers both placement and retrieval logic.
Required Inputs: [CUSTOMER_NAME]; yard layout data (bay names, crane types) if available from project metadata
Safe Inferences: Two primary flow sequences: (a) Placement/Storage flow: Material produced → HSM L1 sends coil identity to CYMS / Caster L1 sends slab identity to SYMS → Crane picks material → Position sensors detect X, Y, Z → Pick/Drop sensor logs event → CYMS/SYMS records slot location → Slot status updated in digital yard map; (b) Retrieval flow: Operator/system selects target material (manual or auto) → Crane moves to location → Pick sensor logs event → CYMS/SYMS updates slot status → Material dispatched, transferred, or moved
Must Not Invent: Specific system response times, specific sensor thresholds, specific API polling intervals, specific network latency requirements
Suggested Structure: Describe Placement Logic and Retrieval Logic as numbered step sequences. Include integration touch-points at each relevant step. Distinguish manual vs. auto operating modes. Reference the integration layer for L1, Caster, and crane subsystem data feeds. Can describe a process flow diagram (see Diagram Guidance for layered architecture description). No mxGraph XML.
AI Generation Rules: Do NOT duplicate shared knowledge from the context files.
