Role: Image-backed section. User uploads the PMYMS system configuration diagram. This section holds the text companion describing the architecture.
Required: Architecture option (with or without HX Controller); confirmed integration points from project metadata.
Infer: PMYMS server pair (online + backup) at the center. Client workstations accessing via web browser. Integration upward to MES/ERP (SAP). Integration laterally to Plate Mill L2, Furnace L2, Plate Leveller L2. Integration downward to crane subsystem via crane-side hardware and gateway. HX Controller in path to crane system if Unified Gateway option is confirmed. Network connectivity between all components via plant network (type [NETWORK_TYPE] to be finalized). Reference-only caveat is mandatory.
Never invent: IP addresses, port numbers, VLAN details, specific network topology, physical rack layout, or component locations. These are all finalized during detailed engineering.
Structure: 2–3 sentences describing the selected architecture option and how integrations connect to the PMYMS application. State key confirmed integration points. End with: "The above configuration is provided for illustrative purposes only and is subject to modification during detailed engineering to optimize overall system performance and functionality."
