Role: Defines Factory Acceptance Testing (FAT) scope, criteria, and conditions before site deployment. Not explicitly present in the sample TS as a standalone section; content inferred from project completion criteria and standard practice.
Required: Confirmation from project metadata whether formal FAT is in scope; if so, FAT location (SELLER facility or third-party test environment).
Infer: If FAT is confirmed: FAT tests all configured software modules, simulated interface message flows (L2, ERP, Weigher), yard visualization accuracy, stacking rule behavior, user access control, and report generation against simulated data. FAT report issued and signed before hardware shipment to site. Shipment conditional on FAT sign-off. If FAT is not explicitly confirmed in project metadata, do not assume it is in scope.
Never invent: Formal FAT pass/fail thresholds, buyer witness travel requirements, or FAT duration unless confirmed in project metadata.
Structure: If FAT confirmed: bullet list of FAT test items (interface message simulation, yard visualization, stacking rules, user access, reporting) plus FAT report issuance condition. If not confirmed: single statement noting that factory acceptance testing approach is to be agreed during detailed engineering. Reference commissioning and work completion criteria as the acceptance gate.
