# domain_context.txt — Domain Knowledge Template
# Target size: ~800 chars | Max: 1000 chars
# Purpose: What the product IS, core capabilities, business drivers, plant environment
# Audience: LLM prompt context — be dense, factual, and jargon-appropriate
#
# AUTHORING GUIDE:
#   1. What is [PRODUCT_NAME]? (1-2 sentences — definition, not marketing)
#   2. Core capabilities (always present in every deployment)
#   3. Optional/configurable capabilities (include when relevant)
#   4. Business drivers (why customers buy this)
#   5. Plant/site environment (existing systems, OT/IT boundary, read-only vs. write access)
#
# DELETE this header block before saving as the live domain_context.txt
# ─────────────────────────────────────────────────────────────────────────────

[PRODUCT_NAME] — [ONE_SENTENCE_DEFINITION]. Sits at [LAYER_DESCRIPTION]. NOT [WHAT_IT_IS_NOT].

Core capabilities (all deployments): [CAPABILITY_1]; [CAPABILITY_2]; [CAPABILITY_3]; [CAPABILITY_4]; [CAPABILITY_5].

Optional components: [OPTIONAL_COMPONENT] — [WHEN_USED]. Use only when [CONDITION].

Business drivers: [DRIVER_1], [DRIVER_2], [DRIVER_3], [DRIVER_4]. [PRODUCT_NAME] never guarantees [EXCLUDED_COMMITMENT].

Plant environment: [ENVIRONMENT_DESCRIPTION]. [NETWORK_TOPOLOGY]. [PRODUCT_NAME] integration is [READ_WRITE_SCOPE] — it [CONSTRAINT].
