# gantt_context.txt — Scheduling & Timeline Template
# Target size: ~400 chars | Max: 600 chars
# Purpose: Project phases, milestones, timeline guidance for Gantt sections
# Audience: LLM prompt context — timeline-precise, draw.io-aligned
#
# AUTHORING GUIDE:
#   1. Phase structure (M1–M5 or product-specific gates with week durations)
#   2. Critical path notes (key dependencies, buyer-gated milestones)
#   3. Shutdown Gantt applicability (is shutdown required? week-window scope)
#   4. Draw.io format guidance (week-based JSON format, GanttTask structure)
#
# DELETE this header block before saving as the live gantt_context.txt
# ─────────────────────────────────────────────────────────────────────────────

Phase structure: [PHASE_1] (~[W]w) → [PHASE_2] (~[W]w) → [PHASE_3] (~[W]w) → [PHASE_4] (~[W]w) → [PHASE_5] (~[W]w). Total indicative: ~[TOTAL]w.

Critical path: [GATE_1_DESCRIPTION] gates [PHASE_GATED]. [GATE_2_DESCRIPTION] gates [PHASE_GATED_2]. Buyer prerequisite delays cause proportional slippage.

Shutdown Gantt: [REQUIRED/NOT_REQUIRED — reason]. [If required: scope is [SHUTDOWN_SCOPE] within [WINDOW_DESCRIPTION].]

Draw.io format: week-based GanttTask JSON — fields: task, phase, start_week, duration_weeks, milestone (bool), dependencies[].
